Sorting by:

<%= link_to 'title', { :controller => 'articles', :action => 'index', :order_by => 'title' } %>
<%= link_to 'word_count', { :controller => 'articles', :action => 'index', :order_by => 'word_count' } %>
<%= link_to 'published', { :controller => 'articles', :action => 'index', :order_by => 'published' } %>
<%= link_to 'top 2', { :controller => 'articles', :action => 'index', :limit => '2' } %>
<br />
<br />


<% @articles.each do |article| %>
  <div class="article">
    <h3><%= article.title %></h3>
    <p>
      <%= raw truncate(article.body, length: 200, separator: '...') %>
      <br>
      <%= link_to 'Show', article %> |
      <%= link_to 'Edit', edit_article_path(article) %> |
      <%= link_to 'Destroy', article, method: :delete, data: { confirm: 'Are you sure?' } %>
    </p>
  </div>
<% end %>
